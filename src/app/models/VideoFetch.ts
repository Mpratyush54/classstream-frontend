import { Time } from "@angular/common";

export interface VideoFetch {
 username:string,
 ChapterName:string,
 ChapterNumber:Number,
 class:Number,
 id:string,
 process:Number,
 thumnail:number,
 time:Time,
 title:string,
 url_thumnail:string,
 url_video:Number,
 video:Number,
}